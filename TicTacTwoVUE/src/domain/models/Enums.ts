export enum PlayerMark {
    X = 'X',
    O = 'O'
}

export enum Direction {
    Up = 'up',
    Down = 'down',
    Left = 'left',
    Right = 'right'
}

export enum MoveType {
    Place = 'place',
    Move = 'move',
    Grid = 'grid'
}

export enum GameResult {
    X = 'X',
    O = 'O',
    Draw = 'Draw'
}
